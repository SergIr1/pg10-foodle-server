description: Create a recipe
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 201
        message:
          type: string
          example: Successfully created a recipe!
        data:
          type: object
          $ref: ../schemas/recipe.yaml
