description: Recipe created
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 201
        message:
          type: string
          example: Recipe created successfully
        data:
          type: object
          $ref: ../schemas/recipe.yaml
